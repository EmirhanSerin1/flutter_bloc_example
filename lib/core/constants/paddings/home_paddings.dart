import 'package:flutter/material.dart';

class HomePaddings{
  static const horizontal = EdgeInsets.symmetric(horizontal: 16);
  static const mainItemsVertical = EdgeInsets.symmetric(vertical: 16);
  static const popularItemPaddings = EdgeInsets.only(left: 16);
}
