import 'package:flutter/material.dart';

class TextFieldProperties{
  static BorderRadius enabledBorderRadius = BorderRadius.circular(10);
  static BorderRadius borderBorderRadius = BorderRadius.circular(10);
  static BorderRadius focusedBorderRadius = BorderRadius.circular(32);

}